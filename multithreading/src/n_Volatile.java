/**
 * The Java volatile keyword can be applied to Java variable declarations to force the
 * Java VM to always read the variable value directly from main memory,
 * and always write changes to the variable directly back to main memory immediately. */
public class n_Volatile {
    volatile  int n = 110;
    volatile  String s = "110";
    volatile  Object object;


}
