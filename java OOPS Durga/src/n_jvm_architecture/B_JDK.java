package n_jvm_architecture;

public class B_JDK {
    /**
     * The JDK allows developers to create Java programs that can be executed and run by the JVM and JRE.
     * use the JDK compiler to turn your text file into an executable program.
     * Compiled code in Java is known as bytecode, and carries the .class extension.
     *
     * JDK = JRE + JVM.
     * JDK contains the JRE
     * */
}
